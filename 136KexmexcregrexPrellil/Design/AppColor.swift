//
//  AppColor.swift
//  136KexmexcregrexPrellil
//
//  Manual palette (asset names in xcassets). Used while Swift asset symbol
//  extensions are disabled to avoid duplicate Color.* in GeneratedAssetSymbols.
//

import SwiftUI

extension Color {
    static let appBackground = Color("AppBackground")
    static let appSurface = Color("AppSurface")
    static let appPrimary = Color("AppPrimary")
    static let appAccent = Color("AppAccent")
    static let appTextPrimary = Color("AppTextPrimary")
    static let appTextSecondary = Color("AppTextSecondary")
}
